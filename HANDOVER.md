# 交接 Handoff — 夜间开发交接说明

**给早上醒来的你。**

先说结论：**它能用了。**现在打开浏览器，说一句人话，它会给你做出东西来。

---

## 30 秒上手

```bash
cd /Users/imac/260912/handoff
npm install
npm start
```

打开 **http://127.0.0.1:8787**

不用注册、不用填 Key（你装的 Cherry Studio 里的 Key 会被自动读到）、不用联网配置。
想先看看效果不想花钱：

```bash
HANDOFF_DEMO=1 npm start
```

离线跑通全流程，不调用任何模型。

---

## 它是什么

**把普通人说不清楚的需求，变成 AI 团队真正交付的成果。**

不是 ChatGPT 套壳。差别在于：用户只说一句人话，比如

> 帮我把这份租房合同看一遍，我怕有坑。押金两个月6000元，租期一年，提前退租押金不退，
> 水电燃气租客承担，维修超200元租客承担。

然后有 8 位"同事"依次接手：

| 谁 | 岗位 | 干什么 |
|---|---|---|
| 小接 🫱 | 接待员 | 听懂你真正想要什么 |
| 小方 📋 | 项目经理 | 拆成能干活的步骤，定交付什么 |
| 小查 🔍 | 调研员 | 补齐需要的背景知识和常识 |
| 小做 ✍️ | 执行专员 | 真正把东西写出来 |
| 小挑 🔎 | 审查员 | 专门挑自己的毛病（挑得挺狠） |
| 小改 🛠️ | 执行专员 | 按挑出来的问题改一遍 |
| 小验 ✅ | 质检员 | 代表你逐条验收 |
| 小交 📦 | 交付专员 | 告诉你拿到东西后怎么用 |

**四个我们坚持的设计：**

1. **看得见**：每个阶段实时显示谁在做、做了多久、说了什么。
2. **诚实**：明确列出"我们替你假设了什么"。假设错了，补一句话就重做。
3. **不烦人**：默认**先做，把假设亮出来**。不反问一堆问题。
4. **抗摔**：模型降级链 + 重试 + 时间预算 + 崩溃恢复。普通任务不会因为网络抖动失败。

---

## 现在的真实状态（我去跑过的，不是猜的）

| 项目 | 实测结果 |
|---|---|
| 单元 + 集成测试 | **403 passed / 8 expected-fail / 1 skipped**，连续多次一致 |
| 端到端冒烟 | `node scripts/smoke.js` → 退出码 0，5.9 秒 |
| **真实模型端到端** | `node scripts/dry-run.js` → **全部通过 ✅**，8 阶段、3 份交付物、质检 `pass_with_notes` |
| 真实模式集成测试 | `HANDOFF_INTEGRATION=1 npx vitest run tests/e2e/pipeline.test.js -t "真实 goal"` → **197 秒跑到 done**（修复前是 712 秒后 failed） |
| 真实模型质量 | 质检员会抓到**自己产出的**细节不一致（标题写"四件事"实际列了五项）—— 它不是走过场 |
| 单阶段耗时 | 接待 10s / 项目经理 19s / 调研 28s / 执行 28s（长文本协议改动前是 100~300s） |
| 详情页响应 | p95 **4.86ms**（含 3×3000 字交付物） |
| 内存 | 删除路径零泄漏；缓存上限 200 任务 |
| 安全 | Key 泄漏扫描（7 端点 + 日志 + 数据目录）全 clean；演示模式 8 次采样零外连 |
| 浏览器 | 产品经理用真实 Chromium 走完 12 条路径（含 5 条失败路径） |

**规模**：代码 18,900+ 行 ｜ 文档 9,038 行 ｜ 直接依赖 3 个（运行期只有 express）

---

## 今晚发生了什么（按价值排序）

我组织了一个 14 人的"虚拟公司"（1 个负责人 + 13 个专业子代理），
按 后端 → 前端 → 安全 → QA → 对抗性测试 → 性能 → 产品 → 文档 → 语言 → 验证 的顺序推进，
每一轮都在 **开发 → 测试 → 评审 → 安全检测 → 修漏洞 → 再开发** 之间循环。

### 最有价值的五件事（都是"看起来没问题、实际会毁掉产品"的那种）

1. **前端和后端之间那根线，没人测。**
   服务端返回 `{job:{...}}`，前端按扁平读 `job.id`。
   结果：真实浏览器里点「开始，交给团队」**100% 失败**，详情页永远空白。
   而**两侧的单测都是绿的** —— 前端只测 `api.js` 层（用手写假响应），
   后端 e2e 全部直打服务端（绕过前端代码）。
   现在补了 `tests/unit/api-contract.test.js`，专门用真实 Express 响应喂给前端。

2. **让模型把 2000 字中文 markdown 塞进 JSON 字符串，是一条死路。**
   实测：每个阶段要重试三四轮、跨两三个模型、跑 100~300 秒。
   对普通人就是"它卡住了"。
   改成 **`<<<ARTIFACT>>>` 定界符 + 纯文本正文**之后：**一次就过，28 秒**。
   这是今晚单个收益最大的改动（`src/llm/text-protocol.js`）。

3. **`sweepTmpFiles()` 会删掉正在写的临时文件。**
   它每 30 秒跑一次，而 `atomicWrite` 用固定名 `${file}.tmp` 且中间要 fsync。
   后果：`rename` 报 ENOENT → 用户「新建任务」直接 500。
   性能工程师压测实测：100 个任务里 7 个失败。
   修法：唯一临时文件名 + 只清 5 分钟前的残渣。

4. **并发 `retry` 全部返回 200。**
   check-then-act 竞态：先 `await load()`、再 `running.has()`、最后 `running.set()`，
   两个请求都能穿过检查。真实模式下**用户的模型费翻倍**，而且前一条流水线变成
   取消不掉的孤儿。修法：在任何 await 之前用同步占位（JS 单线程，天然原子）。

5. **质检判 `needs_revision` 就被标成"任务失败"是产品判断错误。**
   用户等了 4 分钟，明明拿到 3 份有用文档，却被告诉"失败" —— 他连东西都看不到。
   现在分层：**真正失败 = 没有可用的东西**；有东西但质检提了意见 → 交付 + 如实标注。

### 另外修掉的（都有回归测试钉住）

竞态 2 处、路径/编码 3 处、`.env` 从来没被加载、`grade` 字段、伪造的"已修正"通知、
崩溃恢复（卡在 running 的任务）、前端把 404 说成"检查 Wi-Fi"、
畸形 URL 让整个路由卡死、`goal` 传对象存成 `[object Object]`、测试 flaky 根因。

### 我做过但**回退**的一件事（写下来免得你或者下一个人重走）

性能工程师实测缓存占 66~185MB，建议"只让最近 30 个任务在内存里留正文"。
我试着做了，然后**回退**了 —— 因为出现了"详情页拿到空正文"的路径，
也就是说用户打开一个老任务会看到**空白交付物**。

**"用户能不能看到自己的成果"是不可交易的。**省 50MB 换这个风险不划算。
理由已写进 `src/store/json-store.js` 的注释里，连"下次要试就先补哪条测试"都写了。

---

## 06:00 之后又做的事（第二轮：把"偶发失败"从根上治掉）

深夜的几次真实干跑暴露了一个**不容易发现、但会直接毁掉体验**的模式：
任务跑 8 分钟还没结束，最后失败。追下去发现是同一个根因的四种面貌：

| 现象 | 根因 | 修法 |
|---|---|---|
| `draft` 的第三份交付物被截断在半句话上 | `maxTokens: 8000` 不够写 3 份 2000 字文档 | 提到 24000 |
| `research` 阶段直接失败，`finish_reason=length` | 推理型模型先花掉大量 completion token 思考，6000 不够 | 提到 16000 + 明确限制单条长度 |
| 一个审查阶段跑 200 秒还没结束 | 提示词近万字（每份交付物塞 6000 字），模型在长上下文里超时 | 压到每份 1500 字；`verify` 改成**只看首尾**（判断质量不需要通读） |
| 整个任务 8 分钟不收敛 | 「单次超时 × 重试 × 降级链接数」层层放大 | 收紧重试、分阶段超时、**单阶段 5 分钟硬上限**、降级链从 5 级收到 3 级 |

**最重要的一条认知**：`maxTokens` 给大**没有代价**（按实际用量计费，没写满不要钱），
给不够才是灾难。我一开始为了"省时间"把它压小，结果反而更慢 ——
模型把预算全花在思考上，正文是空的（`finish_reason=length`），
于是重试、换模型，一个阶段烧掉两百秒。**这是今晚第二个反直觉的教训。**

还有一个产品判断：`deliver` 是最后一步，而用户要的东西那时**已经做好了**。
它失败绝不该把整个任务判为失败。现在会兜底合成一份「怎么用」
（内容全部来自 plan 里的真实数据），把成果交出去。

---

## 它现在还不够好的地方（诚实清单）

- **模板点下去只填一个标题**。`templates/*.json` 里写好的 `goalTemplate`、
  `placeholders`、`tips`、`notice` 前端一个字都没读。那句
  「⚠️ 我们不是医生，不能诊断…」是全项目最好的文案，用户看不到。
  **这是我认为最该先修的一处**，因为文案和结构都已存在，只是没接线。
- **历史卡片同时显示「已完成」和「准备中」**（列表接口没给 `stages`）。
- **演示模式的产物与用户问的问题无关**（固定产出合同审查样例）。
- **"看一个演示"按钮在空输入框上会报错** —— 而这正是不知道说什么的新用户第一个会点的按钮。
- **首屏顺序反了**：救命的那一屏（场景模板）在首屏之外要滚动，空输入框在首屏。
- 没有取消、没有删除、"不满意重做"的入口（后端接口都有，前端没用）。
- 报错文案没有翻译通道：`src/llm/gateway.js` 的技术报错会原样显示给用户
  （会看到「模型输出的结构不符合要求：$.confidence 取值必须是…」）。
  **语言工程师给了 42 条改造清单和 118 条可直接应用的文案建议**（`docs/runtime-copy.json`）。
- 8 个测试用例是 `it.fails` —— **它们绿着恰恰说明缺陷还在**，见 `docs/BUGS.md`。

详细的待办在 `docs/reports/S11-产品审计.md`（产品）、`docs/BUGS.md`（对抗性测试）、
`docs/reports/S13-语言质量.md`（文案）里，每条都有复现和具体改法。

---

## 如果你想继续推进，我建议这个顺序

1. **接通模板**（读 `goalTemplate`/`placeholders`/`tips`/`notice`，做变量替换）
   —— 投产比最高，文案和结构都已存在
2. **错误消息翻译通道** + 应用 `docs/runtime-copy.json` 里的建议
   —— 一次改动让 20 条错误路径一起变好
3. 首页顺序调整 + 演示按钮在空输入框可用 + 演示内容跟随用户目标
4. 修 `docs/BUGS.md` 里剩下的 `it.fails`（把它当待办清单，修一条删一条）

---

## 想深入了解

| 文档 | 内容 |
|---|---|
| `README.md` | 面向用户的产品说明 |
| `docs/PLAYBOOK.md` | 12 个生活场景，零技术术语（写给普通人） |
| `docs/ARCHITECTURE.md` | 架构、数据流图、14 条技术债 |
| `docs/DECISIONS.md` | 20 条 ADR，每条含"我们放弃了什么" |
| `docs/BUGS.md` | 缺陷登记（对抗性测试找到的） |
| `docs/TESTING.md` | 怎么跑测试、测了什么、哪里没测 |
| `docs/SECURITY.md` | 安全说明（写给普通人） |
| `docs/PERFORMANCE.md` | 性能画像与已知瓶颈 |
| `docs/CONTRACT.md` | 工程契约（**已降级为架构基线**，§0 列了 12 条与代码的偏离） |
| `docs/reports/` | 13 份工作报告，每份都含真实命令与输出 |
| `docs/VERIFICATION.md` | 别人照着能重做全部验证的清单 |

---

## 一句我想说的话

今晚这个项目里，**最值钱的产出不是代码，是那些"测试全绿但产品其实是坏的"的发现。**

前端和后端之间那根线、正在写的临时文件、并发点的两次点击、一句让用户白等 4 分钟的
`needs_revision` —— 这些东西任何一处的单测都不会红。它们是被**真的用一遍**找出来的。

所以如果只能给这个项目留一条经验，我会留这条：

> **别相信"测试都过了"。去真的用一次。**

---

*祝你早上好。*
