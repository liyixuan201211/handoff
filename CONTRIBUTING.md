# 参与贡献

谢谢你想帮这个项目。它想做的事很简单：**让不懂 AI 的普通人也能把事办成。**

下面是你需要知道的事。都不长。

---

## 先跑起来（2 分钟）

```bash
git clone <repo> && cd handoff
npm install
npm test          # 516 个测试，不需要 API Key，不需要联网
npm start         # 打开 http://127.0.0.1:8787
```

**重要的一点**：测试套件**完全不依赖任何 API Key、不依赖 Cherry Studio、不调真实模型**。
你 clone 下来就能跑绿。这是我们刻意维持的 ——
如果哪天你发现某条测试需要真 Key 才能过，那是个 bug，请提 issue。

想看不花钱的完整流程：

```bash
HANDOFF_DEMO=1 npm start
```

---

## 项目结构

```
src/
  llm/          模型网关：重试、降级链、JSON 抢救、长文本协议
  pipeline/     流水线引擎与 8 个阶段
  prompts/      提示词库（产品的灵魂，改动前请读注释）
  tools/        工具系统：注册表、网络闸门、原生工具、MCP 客户端、工具循环
  skills/       Skill 加载与匹配
  security/     输入净化、输出审计
  store/        JSON 持久化、事件总线
  routes/       HTTP 路由与 SSE
public/         零构建的前端（原生 ES Module）
docs/           契约、架构、决策记录、各岗位报告
skills/         领域知识包（欢迎贡献这里！）
```

---

## 最有价值的三种贡献

### 1. 写一个 Skill（最欢迎，门槛最低）

技能是 markdown，不是代码。**这是刻意的设计**：
这个产品的核心资产是**领域知识**，而领域知识最好由懂那件事的人来写。
一个护士能写"怎么跟医生沟通"，但她不会写 TypeScript。

在 `skills/<技能名>/SKILL.md` 里写：

```markdown
---
name: 租房纠纷
description: 处理押金退还、房东违约、维修责任等租房纠纷
when: 用户遇到租房相关的麻烦
---

## 处理租房纠纷的经验

1. **先固定证据**：转账记录、聊天记录、入住照片。
   ...
```

写什么最有价值：**你在某个领域踩过的坑**。
"签合同前一定要问这三件事""跟物业交涉这样说才有用"——
这些是工程师写不出来的东西。

### 2. 报告"这里对普通人不好用"

我们最怕的不是 bug，是**"能跑但普通人用不了"**。

如果你用的时候卡住了、看不懂某句话、不知道该点哪里 —— **那是有价值的反馈**。
最好的形式是一个 issue，包含：

* 你做了什么（具体到点了哪个按钮）
* 你期待发生什么
* 实际发生了什么（截图最好）
* 如果方便，说明你的情况（家里长辈用？自己不熟悉电脑？）

### 3. 修 bug / 加功能

改动前请先读：

| 文档 | 什么时候读 |
|---|---|
| `docs/CONTRACT.md` | 想知道"当时为什么这么设计"（**注意：它是架构基线，不是硬约束，冲突以代码为准**） |
| `docs/ARCHITECTURE.md` | 想理解数据怎么流动 |
| `docs/DECISIONS.md` | 想知道某处为什么这么做、放弃了什么 |
| `docs/TOOLS.md` | 要动工具或 MCP |
| `src/prompts/index.js` 顶部注释 | 要改提示词 |

---

## 代码约定

### 必须遵守

**1. 不引入不必要的依赖。**
运行期只有 `express` 一个直接依赖（加 MCP SDK）。
每加一个包就多一份审计面。想加之前先问：能不能用 Node 内置的做？

**2. 前端不引入构建步骤。**
`public/` 里是原生 ES Module，浏览器直接加载。
`npm start` 之后改前端代码刷新即可生效。这是刻意的：**少一层构建就少一类故障**。

**3. 安全默认必须是"关"。**
新增任何能触达外界的能力（网络、文件、进程），默认值必须是关闭 + 显式配置才开。
理由见 `src/tools/config.js` 顶部。

**4. 注释写"为什么"，不写"是什么"。**
这个项目的注释密度偏高，是刻意的 ——
因为我们踩过的坑（`try/catch` 静默吞掉 ReferenceError、IPv6 方括号绕过 SSRF、
`maxTokens` 给太小反而更慢）如果不写下来，下一个人会重踩一遍。
**如果你修了一个"看起来很奇怪"的 bug，请在代码里留下它的故事。**

**5. 错误消息要给人话。**
参见 `docs/COPY-GUIDELINES.md`。我们的用户是护士和小店主，
他们看到"模型输出的结构不符合要求：$.confidence 取值必须是..."只会关掉页面。

### 测试要求

```bash
npm test                    # 全部
npm run test:unit           # 单元（快，不联网）
npm run test:e2e            # 端到端（含离线 demo 模式）
node scripts/smoke.js       # 冒烟（5 秒）
node scripts/dry-run.js     # 真调模型跑完整流水线（花钱、几分钟）
node scripts/bench.js       # 性能基准
```

新增功能请配测试。**特别欢迎"这个应该被拒绝"的测试** ——
安全边界比功能更容易写错，而写错时后果更严重。

如果你修了一个 bug，请先写一条**会红的测试**，再修。
这个仓库里很多测试就是"某次踩坑之后钉下来的"，注释里写着当时发生了什么。

---

## 提交 PR

* 分支：`fix/xxx` 或 `feat/xxx`
* commit message：中文或英文都行，说清**为什么**改
* PR 描述里请写：**这个改动会让哪个具体的普通人少遇到什么麻烦？**

CI 会跑：全部测试（不调真实模型）+ 冒烟 + JSON 语法 + **密钥泄漏扫描**。

---

## 关于密钥

**永远不要把 API Key 提交进来。**

* `.env` 和 `handoff.config.json` 已经在 `.gitignore` 里
* 本地 Key 会自动从环境变量或 Cherry Studio 配置读取
* 测试里用的都是 `sk-test-...` 这类明显的假 key
* CI 有一条专门的扫描，发现疑似真实密钥会直接失败

如果你不小心提交了真实密钥：**先去服务商那里吊销它**，再处理 git 历史。

---

## 一件事想请你记住

这个项目的用户是**李姐（48 岁护士）、老王（55 岁小店主）、张姨（62 岁退休）**。

他们不会读文档、不愿意填表、看不懂术语、**一次失败就再也不用了**。

所以：

> 判断一个改动好坏的标准不是"功能实现了没有"，
> 而是**李姐会不会因为它少卡住一次**。

---

## 行为准则

就一条：**对人友善，对问题严格。**

我们欢迎"你这个设计对普通人不友好"这样的意见 —— 那是这个项目最需要的反馈。
