# 交接 Handoff — 环境变量（全部可选！）

本产品**默认零配置**：启动时会自动从你本机的 Cherry Studio 配置里读取 API Key。
只有在你没装 Cherry Studio、或者想用自己的 Key 时，才需要下面这些。

# ── 模型提供方 ────────────────────────────────────────────────
# AI Ping（默认，提供 DeepSeek-V4.1-Flash 等）。留空则自动读 Cherry Studio。
AIPING_API_KEY=
AIPING_BASE_URL=https://aiping.cn/api/v1

# DeepSeek 官方（可选）
DEEPSEEK_API_KEY=

# 逗号分隔的模型降级链，从左到右尝试
HANDOFF_MODEL_CHAIN=deepseek-flash,aiping/DeepSeek-V4-Flash,aiping/DeepSeek-V4.1-Flash,aiping/GLM-5.3,aiping/Qwen3.8-Max

# ── 服务 ─────────────────────────────────────────────────────
HANDOFF_PORT=8787
HANDOFF_HOST=127.0.0.1

# 持久化目录（默认 ./data）
HANDOFF_DATA_DIR=./data

# 演示模式：1 = 完全不调模型，用内置样例数据跑通全流程（离线演示/评测用）
HANDOFF_DEMO=0

# 单次模型调用超时（毫秒）
HANDOFF_LLM_TIMEOUT_MS=120000
